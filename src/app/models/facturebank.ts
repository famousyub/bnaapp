export interface Facturebank {

    id?: number; // Optional field, if your facture has an ID

    nom: string;
    prenom: string;
    amount: number;
    factname: string;
    niveau: string;
    photo: string;
}
